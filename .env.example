# Azure AI Foundry Agent — Responses API endpoint for the agent
AGENT_URL=

# Service Principal used locally by DefaultAzureCredential.
# In App Service these are omitted — the managed identity is used instead.
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=

# Optional
AZURE_SCOPE=
DEBUG_AGENT=
PORT=
